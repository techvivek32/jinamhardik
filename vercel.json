{
  "buildCommand": "pnpm --filter @workspace/jinambe run build",
  "outputDirectory": "public",
  "installCommand": "npm install -g pnpm && pnpm install",
  "framework": null,
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
